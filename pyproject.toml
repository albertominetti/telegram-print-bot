[project]
name = "telegram-print-bot"
version = "0.1.0"
description = "Telegram bot that prints received PDFs to a Windows shared printer"
requires-python = ">=3.14"
dependencies = [
    "python-telegram-bot==22.7",
    "aiofiles>=25.1.0",
    "img2pdf>=0.5.1",
]

[dependency-groups]
dev = []
